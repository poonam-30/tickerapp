const Primary = ({clickEvent})=>{
    return (
    <>                  
        <div className="modalFullBtn button">
            <button onClick={clickEvent}>Continue</button>
        </div>
   </>
    )
}

export default Primary;